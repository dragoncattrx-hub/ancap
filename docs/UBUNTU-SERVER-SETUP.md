# Ubuntu server setup

Production deploy for ancap.cloud uses Docker Compose on the migration host (`/opt/ancap-migration/current`). Prefer:

- `docker-compose.prod.yml`
- `docs/` deploy / ops notes in the monorepo
- GitHub Actions workflow `Deploy ancap.cloud`

This stub keeps Docs CI link checks green for historical Ubuntu setup references.

import { StardustLegalView } from "@/components/legal/LegalViews";

export const metadata = {
  title: "StardustSRT weather control — legal notice | ANCAP",
  description:
    "ANCAP Stardust desk: ACP-settled weather-control and earth-monitoring partner literacy only. Not a live geoengineering console, not official meteorological warnings, not a guaranteed weather outcome.",
};

export default function LegalStardustPage() {
  return <StardustLegalView />;
}

import { LightChamberLegalView } from "@/components/legal/LegalViews";

export const metadata = {
  title: "Vinci light chamber — legal notice | ANCAP",
  description:
    "AETERNA Vinci light / photobiomodulation chamber: licensed phototherapy partner briefs only. Not a medical device, not a reconstructed Leonardo invention, not a safe-tanning claim.",
};

export default function LegalLightChamberPage() {
  return <LightChamberLegalView />;
}

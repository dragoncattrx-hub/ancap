import { DnaPheromonePerfumeLegalView } from "@/components/legal/LegalViews";

export const metadata = {
  title: "DNA-matched pheromone perfume — legal notice | ANCAP",
  description:
    "AETERNA DNA-matched pheromone perfume: sold per bottle (8,900 ACP) via licensed cosmetic / fragrance partner. Not a drug, not an aphrodisiac claim, not CE/FDA pheromone medicine, genome bytes never uploaded.",
};

export default function LegalDnaPheromonePerfumePage() {
  return <DnaPheromonePerfumeLegalView />;
}

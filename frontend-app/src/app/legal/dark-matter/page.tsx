import { DarkMatterLegalView } from "@/components/legal/LegalViews";

export const metadata = {
  title: "Dark matter auction — legal notice | ANCAP",
  description:
    "ANCAP dark-matter auction: symbolic ACP cosmology title RFQs only. Not physical dark-matter ownership, not sovereignty, not a scientific detection certificate.",
};

export default function LegalDarkMatterPage() {
  return <DarkMatterLegalView />;
}

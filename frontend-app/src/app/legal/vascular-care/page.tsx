import { VascularCareLegalView } from "@/components/legal/LegalViews";

export const metadata = {
  title: "Vascular Care — legal notice | ANCAP",
  description:
    "AETERNA Vascular Care: licensed phlebology/aesthetic partner briefs only. Not a medical device, not surgery, not a guaranteed vein-diameter claim.",
};

export default function LegalVascularCarePage() {
  return <VascularCareLegalView />;
}

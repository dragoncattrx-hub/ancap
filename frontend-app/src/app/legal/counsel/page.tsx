import { CounselLegalView } from "@/components/legal/LegalViews";

export const metadata = {
  title: "Worldwide legal services — legal notice | ANCAP",
  description:
    "ANCAP worldwide counsel desk: ACP briefs and licensed counsel matching. Not a law firm, not an attorney–client relationship with ANCAP, not a guaranteed legal outcome.",
};

export default function LegalCounselPage() {
  return <CounselLegalView />;
}
